% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matched_comparison.R
\name{matched_comparison}
\alias{matched_comparison}
\title{Matched comparison group}
\usage{
matched_comparison(
  df,
  propscore_df,
  cov_cols,
  arm_col,
  intervention_level,
  replace = T,
  downsample = F
)
}
\arguments{
\item{df}{A data frame containing treatment group and comparison cohort data}

\item{propscore_df}{A data frame with a column for each variable in \code{cov_cols} and a \code{PropScore} column giving the estimated propensity score for each combination of their levels. This should have been returned by \code{propscore_df}.}

\item{cov_cols}{A vector of strings, the column names of the covariates to be matched on. These should all be factor / categorical data.}

\item{arm_col}{The name of the column indicating which rows are treated cases and which are comparison cases. These should be factor or character, with only two levels / options.}

\item{intervention_level}{The value in the \code{arm_col} for the treatment cases}
}
\value{
\code{matched_comparison} returns a data frame containing the (candidate) matched treatment and comparison groups.
}
\description{
Generate a matched comparison group using the propensity score sampling approach
}
